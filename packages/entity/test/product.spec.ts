/* eslint-disable @typescript-eslint/no-unsafe-assignment */
import {dbMappingTest} from './mappingTestTemplate';
import {Product, ProductHistory, ProductImage} from '../src';

dbMappingTest(Product);
dbMappingTest(ProductHistory);
dbMappingTest(ProductImage);
